:- import libwww_fetch_url/4, libwww_form_request/4 from libwww.

%% example for fetching url with wrong authentcation information

urlauw :-
	libwww_fetch_url('http://www.aimr.org/canservices/private',
			 [timeout(3),authorization('613138', 'zhangh')],_R,S),
	writeln(libwww_fetch_url('http://www.aimr.org/canservices/private', 
				 [timeout(3),authorization('613138','zhangh')],
				 _R,S)).

